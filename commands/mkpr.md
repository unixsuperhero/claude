---
description: Create a pull request and track it with hiiro
---

# /mkpr

Run the `/open_pull_request` skill to create a PR for the current branch.

After the PR is created and you have the PR number, run:

```bash
h pr track PR_NUMBER
```

$ARGUMENTS
